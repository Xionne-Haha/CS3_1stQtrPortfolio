# Q1 End of Lessons Reflections


## Topics that I know very well
**1. Javascript Variable Definition** = Variables can be known as something that may change. A number defined by a letter is called a variable in basic algebra. X might be a common name for a variable, but Y, Z, or another name may describe it just as easily. X generally has no value but using variable key words, we can associate a meaning with it.
**2. Javascript Variable Declaration** = There are three types of declaration variables. The first is called var.  Variables declared with var have function scope, meaning they are accessible throughout the function they're declared in, and they are also subject to hoisting, where their declaration is moved to the top of their scope. When we use the let keyword rather than var; it imposes scope constraints. The let declared variables cannot be redeclared. This will give you a SyntaxError if you try to do so. The scope of those variables is the block they are defined in, also in any sub-blocks. The browser will throw a ReferenceError if you are trying to access these variables outside of their block. The const declaration creates a constant whose scope is often either global or local to the declared block during which we declare it. The value of a constant is read-only; you cannot change it through reassignment, and you cannot redeclare it.
**3. Conditional Statements** = If is used to specify actions. Else if is used if the first statement is false. Else is used if all the other statements stated above are false.
**4. Boolean** = A boolean is a data type in JavaScript that represents one of two possible values: true or false. It is most often used to store a "yes/no" or "on/off" state, and it is fundamental to control flow statements like if and while loops.
**5. Switch Statements** = Evaluates an expression and executes a block of code based on a matching case. It’s arguably the best alternative to using if, else if and else statements, allowing your code to be readable and easier to understand. 

## Topics that I need to work on.
**1. Loop** = Even though I can write basic loops here and there, I cannot completely wrap my head around which loop to put and where in the code. I know that the basic definition of a loop is that it’s a programming construct that repeatedly executes a block of code as long as a specified condition is true. It allows you to automate repetitive tasks without writing the same code multiple times, saving time and making your code more efficient.
**2. Function** = For no reason whatsoever, I cannot, for the life of me, fully understand how functions work. Like loops, I can also make basic functions but integrating it into an actual code somehow requires me to look at a guide or something to actually accomplish it. The basic definition of a function is that a function is a block of reusable code designed to perform a specific task. Defining a function once allows you to call it whenever needed, which helps to organize your code and avoid repetition.
**3. Relational and Logical Operators** = Sometimes, I forget what specific symbols mean. Like for example, I sometimes fail to distinguish the difference between “==” and “===”. Comparison operators are used in conditional statements to compare different values.
**4. Nested Loop** = Like the others, I can’t fully process Nested Loops properly but Nested loops are a type of loop that is made up of many loops. The most popular type of nested loop is one loop within another. “Outer loop” is the first loop and the “inner loop” is the second loop. The outer loop executes first, followed by the inner loop, which executes inside the outer loop each time the outer loop runs. 
**5. Number Properties** = I didn’t really pay attention to this specific segment of the topic but the basic definition for Number Properties is this: Number properties are built-in attributes and methods available on all JavaScript number values. They provide helpful tools for manipulating and checking numbers, such as converting them to strings or determining if a value is an integer.


**Methods to help myself understand the items listed above:** Self-Study and use the learning guides since the explanations are located there. 

**Two methods that are effective in helping me understand the topics:** Step-by-step explanation of the codes and all four people collaboration in the tables.

**Two methods that are NOT effective in helping me understand the topics:** That one time that random students were asked on what topic they learned but mainly, that was my fault since I wasn’t really paying attention that time and my brain short-circuited and for the second one is that maybe the lessons were moving too fast? (that one is not really that true because for one, I can understand the topics too and for second, I just can’t think of something that does NOT help me in Computer Science)
